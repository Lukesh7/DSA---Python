# Heap - min & max 

# min heap - smallest element should be parent node

import heapq

heap = [] #empty list

heapq.heappush(heap, 10) #adds to heap
heapq.heappush(heap, 5)
heapq.heappush(heap, 15)

print(heap) #[5, 10, 15]

print(heapq.heappop(heap)) #5 (min)
